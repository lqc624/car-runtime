# ADR-001 · max-tokens 处置策略
状态: 已定稿（S2 实装）| 2026-09-02
决策: 默认 DSH 式解析前收口（finishReason=length 不解析工具调用，禁补全半截 JSON）；TurnEndReason 保留至 turn/end（endReasonTrail）；只读白名单工具可选 Pi 式补错重试（preset 显式开启）；finishReason 为不可变透传字段，改写/吞没触发 AL-05。
证据: D5 §4 + SR-17（vLLM #53269 教训）+ POC-3 I4 断言（截断保留语义已验证）。
